#include <stdlib.h>
#include <stdio.h>

int main(){
    int par;

        printf("Digite um valor inteiro: \n");
        scanf("%d", &par);

            if(par % 2 == 0)
            printf("Este numero e par");
            else
            printf("Este numero e impar");
}